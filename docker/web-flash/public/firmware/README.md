# Place the merged flash image here as:
#   plane-radar.bin
#
# From the repo root:
#   ./scripts/merge-firmware.sh -o docker/web-flash/public/firmware/plane-radar.bin
#   echo "1.2.0" > docker/web-flash/public/firmware/VERSION
#
# Or copy a GitHub Release asset and rename it to plane-radar.bin.
